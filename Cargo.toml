[package]
name = "directory"
version = "0.9.5"
authors = ["El RIDO <elrido@gmx.net>"]
edition = "2021"
license = "AGPL-3.0"

[dependencies]
diesel_migrations = "1.4.0"
dns-lookup = "1.0.8"
futures = "0.3.25"
isocountry = "0.3.2"
lazy_static = "1.4.0"
maxminddb = "0.23.0"
rand = "0.8.5"
regex = "1.7.0"
tokio = "1.24.2"
url = "2.3.1"

[dependencies.diesel]
version = "1.4.8"
features = ["sqlite"]

[dependencies.hyper]
version = "0.14.23"
features = ["client"]

[dependencies.hyper-rustls]
version = "0.23.2"
default-features = false
features = ["native-tokio", "http1", "http2", "tls12", "webpki-tokio"]

[dependencies.libsqlite3-sys] # included for the static binary to link it in
version = "0.25.1" # this version needs to be compatible with diesel
features = ["bundled"]

[dependencies.rocket]
version = "0.5.0-rc.2"
features = ["json"]

[dependencies.rocket_dyn_templates]
version = "0.1.0-rc.2"
features = ["tera"]

[dependencies.rocket_sync_db_pools]
version = "0.1.0-rc.2"
features = ["diesel_sqlite_pool"]

[profile.release]
opt-level = 'z'  # Optimize for size.
lto = true
codegen-units = 1
strip = true